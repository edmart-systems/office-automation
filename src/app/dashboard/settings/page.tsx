import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Settings | Office X",
  description: "Office Automation System",
};

const SettingsPage = () => {
  return <div>SettingsPage</div>;
};

export default SettingsPage;
